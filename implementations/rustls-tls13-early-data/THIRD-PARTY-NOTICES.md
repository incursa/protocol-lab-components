# Third-party notices

This component pins `rustls` 0.23.35, `rustls-rustcrypto`
0.0.2-alpha, `rustls-pemfile` 2.2.0, `serde` 1.0.228,
`serde_json` 1.0.149, and `sha2` 0.10.9. Their transitive dependency versions
are locked by `source/Cargo.lock`.

Built package archives contain `third-party-licenses/index.json` plus the
license, copying, and notice files exposed by every package in the locked Cargo
metadata. This notice records implementation provenance only; it does not add
public protocol authority.
