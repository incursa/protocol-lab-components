# Go classic DNS TCP executor

This binding-specific executor supports only the exact two-octet-framed TCP A calibration identity. It rejects UDP, the UDP-triggered retry identity, secure-DNS, unknown, and substituted identities.
