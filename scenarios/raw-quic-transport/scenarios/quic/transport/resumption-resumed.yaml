schemaVersion: "1.0"
title: QUIC Resumption Resumed
status: deferred
kind: protocolValidation
layer: transport
roles:
  - server
requires:
  capabilities:
    - quic.server
  protocols:
    - quic
  roles:
    - server
trafficShape: handshake-only
id: quic.transport.resumption.resumed
name: QUIC Resumption Resumed
family: quic.transport
description: Reserved raw QUIC scenario for proving that a resumed connection is accepted and classified as resumption.
protocol: quic
implementationRole: server
requiredCapabilities:
  - quicTransport
  - quicHandshake
  - quicResumption
quicTransport:
  behavior: resumption-resumed
  connectionCount: 1
  streamType: bidirectional
  streamCount: 0
  payloadSizeBytes: 0
  payloadDirection: client-to-server
  openPattern: sequential
  expectedBytes: 0
validation:
  required: true
  checks:
    - unsupported-until-quic-validator
    - resumption-resumed-recorded
benchmark:
  warmupSeconds: 2
  durationSeconds: 10
  repetitions: 1
  connections:
    - 1
  streamsPerConnection:
    - 0
benchmarkCompat:
  compatibleLoadShapes:
    - fixed-path-request-response
  primaryMetrics:
    - connectTimeMeanMs
    - errorRate
artifacts:
  required:
    - validation.json
    - result.json
  optional:
    - qlog
    - counters
networkProfile: clean
requiredMetrics:
  - quic.connection.resumptionAccepted
artifactRequirements:
  - load-tool.stdout.txt
  - load-tool.stderr.txt
tags:
  - quic
  - handshake
  - resumption
