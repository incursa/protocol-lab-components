executorIdentity: {id: go-http2-websocket-executor, name: Go HTTP/2 RFC 8441 WebSocket Test Executor, version: 0.1.0, vendor: Incursa}
versionCompatibility: {contractVersion: test-executor-v1, compatibleContractVersions: [test-executor-v1], executorVersion: 0.1.0}
claimedCapabilities:
  - {id: http2.websocket.rfc8441.extended-connect, status: supported}
  - {id: http2.websocket.rfc8441.control-frames, status: supported}
  - {id: http2.websocket.rfc8441.text-echo, status: supported}
  - {id: http2.websocket.rfc8441.binary-echo, status: supported}
  - {id: http2.websocket.rfc8441.close, status: supported}
  - {id: http2.websocket.rfc8441.multi-message-text-echo, status: supported}
  - {id: settings-enable-connect-protocol-proof, status: supported}
  - {id: raw-http2-pseudo-header-proof, status: supported}
  - {id: rfc6455-client-masking-proof, status: supported}
  - {id: exact-tls1.3-alpn-h2-proof, status: supported}
  - {id: rfc6455-over-http1, status: unsupported}
  - {id: rfc9220-over-http3, status: unsupported}
supportedTestSelectors:
  - {selectorType: test-id, expression: http2.websocket.rfc8441.extended-connect}
  - {selectorType: test-id, expression: http2.websocket.rfc8441.control-frames}
  - {selectorType: test-id, expression: http2.websocket.rfc8441.text-echo}
  - {selectorType: test-id, expression: http2.websocket.rfc8441.binary-echo}
  - {selectorType: test-id, expression: http2.websocket.rfc8441.close}
  - {selectorType: test-id, expression: http2.websocket.rfc8441.multi-message-text-echo}
supportedScenarioSelectors:
  - {selectorType: scenario-id, expression: http2.websocket.rfc8441.extended-connect}
  - {selectorType: scenario-id, expression: http2.websocket.rfc8441.control-frames}
  - {selectorType: scenario-id, expression: http2.websocket.rfc8441.text-echo}
  - {selectorType: scenario-id, expression: http2.websocket.rfc8441.binary-echo}
  - {selectorType: scenario-id, expression: http2.websocket.rfc8441.close}
  - {selectorType: scenario-id, expression: http2.websocket.rfc8441.multi-message-text-echo}
supportedProtocolFamilies: [h2, tls, websocket]
supportedExecutionModes: [single-cell, process]
requiredTargetEndpointBindings: [{bindingId: primary, purpose: websocket-h2-endpoint, endpointType: https, protocols: [h2, tls]}]
supportedArtifactTypes:
  - {type: validation.json, producedByStates: [stopped, failed]}
  - {type: protocol-proof.json, producedByStates: [stopped, failed]}
  - {type: websocket-summary.json, producedByStates: [stopped, failed]}
  - {type: payload-hash.json, producedByStates: [stopped, failed]}
  - {type: frame-summary.json, producedByStates: [stopped, failed]}
  - {type: http2-frame-summary.json, producedByStates: [stopped, failed]}
  - {type: result.json, producedByStates: [stopped, failed]}
  - {type: http2-websocket-executor-result.json, producedByStates: [stopped, failed]}
  - {type: websocket-load-summary.json, producedByStates: [stopped, failed]}
  - {type: unsupported.json, producedByStates: [failed]}
metricsAvailability: {available: true, availableKinds: [validation, connections-per-second, messages-per-second, bytes-per-second, latency-percentiles, operation-outcomes, effective-streams]}
parser: {id: protocol-lab-websocket-h2-json, type: protocol-lab-websocket-h2-json, preservesRawOutput: true}
telemetry: {supported: true, supported_contract_versions: [telemetry-bundle-v1], supported_scopes: [test-executor, load-generator], supported_artifact_kinds: [summary, metrics, stdout, stderr, raw], export_modes: [inline, artifact-bundle]}
