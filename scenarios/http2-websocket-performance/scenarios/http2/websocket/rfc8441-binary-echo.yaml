schemaVersion: "2.0"
version: 2.0.0
id: http2.websocket.rfc8441.binary-echo
title: RFC 8441 WebSocket Binary Echo
description: Echo a deterministic 1 KiB binary message over WebSocket on HTTP/2.
status: draft
kind: workload
layer: application
protocol: h2
family: websocket
semanticTemplateId: websocket.echo.binary.1kib
protocolStack: [websocket, h2, tls, tcp]
upstreamStandards: [https://www.rfc-editor.org/rfc/rfc6455, https://www.rfc-editor.org/rfc/rfc8441]
roles: [client, server]
requires: {capabilities: [websocket.client, websocket.server, websocket.binary-echo], protocols: [h2], roles: [client, server]}
trafficShape: bidirectional-stream
executionProfile: {transportSecurity: tls, protocolVariant: websocket-h2-extended-connect, implementationRole: origin-server, selectedComponentProofRequired: true, executionModePolicy: record-effective-mode}
websocket: {binding: http2-extended-connect, standard: rfc8441, path: /websocket, subprotocol: null, operation: binary-echo, messageType: binary, messageBytes: 1024, messageCount: 1, payloadGenerator: repeated-octet, payloadOctet: 66, payloadSha256: 9b6ce55f379e9771551de6939556a7e6b949814ae27c2f5cfd5dbeb378ce7c2a, fragmentation: none, masking: client-required, closeCode: 1000, authority: websocket.plab.test, extensions: none, bootstrapPolicy: rfc8441-extended-connect, closeReasonPolicy: empty-utf8-reason, connectionLifecycle: pre-established-connection-per-operation, measuredWindow: message-round-trip, handshake: {binding: http2-extended-connect, settingsEnableConnectProtocol: 1, requestPseudoHeaders: {method: CONNECT, protocol: websocket, scheme: https, authority: websocket.plab.test, path: /websocket}, requestHeaders: {secWebSocketVersion: "13", originPresent: false, secWebSocketProtocolPresent: false, secWebSocketExtensionsPresent: false, secWebSocketKeyPresent: false}, prohibitedConnectionHeadersPresent: false, responseStatus: 200, responseHeaders: {secWebSocketAcceptPresent: false, secWebSocketProtocolPresent: false, secWebSocketExtensionsPresent: false}}}
validation:
  required: true
  checks: [protocol:h2, no-fallback, extended-connect, no-subprotocol, no-extensions, no-connection-headers, no-sec-websocket-accept, no-sec-websocket-key, sec-websocket-version:13, pseudo-protocol:websocket, settings-enable-connect-protocol, message-type:binary, message-size:1024, payload-sha256, ordered-echo, close-code:1000, zero-unexpected-failures, zero-timeouts]
  protocol: {requested: websocket-over-h2, expectedObserved: websocket-over-h2, fallbackAllowed: false}
benchmarkCompat: {compatibleLoadShapes: [smoke, diagnostic, regression, comparison, sustained-messages, soak], primaryMetrics: [messagesPerSecond, bytesPerSecond, messageLatencyP95, messageLatencyP99, failedOperations, timedOutOperations]}
requiredMetrics: [messagesPerSecond, bytesPerSecond, messageLatencyMean, messageLatencyP50, messageLatencyP75, messageLatencyP90, messageLatencyP95, messageLatencyP99, completedOperations, failedOperations, timedOutOperations, totalTransferredBytes, effectiveStreams]
artifacts: {required: [validation.json, protocol-proof.json, websocket-summary.json, payload-hash.json, result.json, load.stdout.log, load.stderr.log], optional: [http2-frame-summary.json, target-telemetry.json, load-generator-telemetry.json]}
availability: {onUnsupported: unsupported, onUnavailable: unavailable, onBindingMismatch: validation-failed, knownUnsupported: [http1-upgrade-fallback, payload-hash-unavailable]}
networkProfile: clean
tags: [http2, websocket, rfc8441, binary, echo]
