# Go classic DNS UDP executor

This binding-specific executor supports exact bare-datagram A calibration and the mandated UDP TC-to-TCP retry identity. It rejects TCP-only, secure-DNS, unknown, and substituted identities. The TCP retry exists only inside the exact truncation contract and does not make this package the TCP A evidence lane.
