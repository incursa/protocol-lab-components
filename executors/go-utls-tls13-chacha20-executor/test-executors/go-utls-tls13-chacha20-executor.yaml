executorIdentity: {id: go-utls-tls13-chacha20-executor, name: Go uTLS TLS 1.3 ChaCha20 Executor, version: 0.1.0, vendor: Incursa}
versionCompatibility: {contractVersion: test-executor-v1, compatibleContractVersions: [test-executor-v1], executorVersion: 0.1.0}
claimedCapabilities:
  - {id: tls.handshake.full.chacha20, status: supported}
  - {id: exact-tls1.3-chacha20-proof, status: supported}
  - {id: exact-client-hello-chacha20-only-proof, status: supported}
  - {id: exact-x25519-proof, status: supported}
  - {id: authenticated-certificate-profile-proof, status: supported}
  - {id: tls-smoke, status: supported}
  - {id: tls.handshake.full, status: unsupported}
  - {id: tls.handshake.resumed, status: unsupported}
  - {id: tls.handshake.full.tls12, status: unsupported}
  - {id: tls.handshake.mutual-auth, status: unsupported}
  - {id: tls.early-data.accepted, status: unsupported}
  - {id: tls.early-data.rejected, status: unsupported}
  - {id: tls.key-update.diagnostic, status: unsupported}
  - {id: tls.record.throughput, status: unsupported}
  - {id: tls.record.coverage, status: unsupported}
supportedTestSelectors: [{selectorType: test-id, expression: tls.handshake.full.chacha20}]
supportedScenarioSelectors: [{selectorType: scenario-id, expression: tls.handshake.full.chacha20}]
supportedProtocolFamilies: [tls]
supportedExecutionModes: [single-cell, process]
requiredTargetEndpointBindings: [{bindingId: primary, purpose: test-endpoint, endpointType: tls, protocols: [tls]}]
supportedArtifactTypes:
  - {type: validation.json, producedByStates: [stopped, failed]}
  - {type: unsupported.json, producedByStates: [failed]}
  - {type: result.json, producedByStates: [stopped, failed]}
  - {type: client-hello-proof.json, producedByStates: [stopped, failed]}
  - {type: protocol-proof.json, producedByStates: [stopped, failed]}
  - {type: tls-negotiation.json, producedByStates: [stopped, failed]}
  - {type: tls-executor-result.json, producedByStates: [stopped, failed]}
  - {type: tls-load-summary.json, producedByStates: [stopped, failed]}
metricsAvailability: {available: true, availableKinds: [validation, handshakes-per-second, latency-percentiles, operation-outcomes, tls-negotiation]}
parser: {id: protocol-lab-tls-json, type: protocol-lab-tls-json, preservesRawOutput: true}
telemetry: {supported: true, supported_contract_versions: [telemetry-bundle-v1], supported_scopes: [test-executor, load-generator], supported_artifact_kinds: [summary, metrics, stdout, stderr, raw], export_modes: [inline, artifact-bundle], metadata: {optional: true, implementation_owned: true, description: The executor emits exact uTLS ClientHello and negotiated TLS proof; the runner owns stdout/stderr and saturation telemetry.}}
