id: grpc-h2-performance-smoke
name: gRPC over HTTP/2 performance contract smoke suite
description: Fast package-neutral validity selection for all four gRPC/H2 RPC cardinalities.
purpose: diagnostic
resultKind: diagnostic
loadProfileId: grpc-h2-smoke
scenarios: [grpc.h2.unary.echo, grpc.h2.server-streaming.echo, grpc.h2.client-streaming.echo, grpc.h2.bidi-streaming.echo]
protocol: h2
notes: Contract selection only; no gRPC runtime or executor support is implied.
tags: [grpc, h2, smoke, non-publishable]
