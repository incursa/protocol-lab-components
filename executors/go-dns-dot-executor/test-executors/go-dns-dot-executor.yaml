executorIdentity: {id: go-dns-dot-executor, name: Go DNS-over-TLS Test Executor, version: 0.3.0, vendor: Incursa}
versionCompatibility: {contractVersion: test-executor-v1, compatibleContractVersions: [test-executor-v1], executorVersion: 0.3.0}
claimedCapabilities:
  - {id: dns.dot.query.a, status: supported}
  - {id: dns.dot.interoperability.query.a, status: supported}
  - {id: dns.dot.resolver.interoperability.query.a, status: supported}
  - {id: secure-dns-smoke, status: supported}
  - {id: exact-dot-tls1.3-proof, status: supported}
  - {id: canonical-dns-wire-proof, status: supported}
  - {id: dns.classic.tcp.query.a, status: unsupported}
  - {id: dns.classic.udp-truncated-tcp-retry, status: unsupported}
  - {id: dns.classic.udp.query.a, status: unsupported}
  - {id: dns.doh2.query.a, status: unsupported}
  - {id: dns.doh2.interoperability.query.a, status: unsupported}
  - {id: dns.doh3.get.a, status: unsupported}
  - {id: dns.doh3.query.a, status: unsupported}
  - {id: dns.doh3.query.aaaa, status: unsupported}
  - {id: dns.doh3.query.cname-chain, status: unsupported}
  - {id: dns.doh3.query.large-dnssec-shaped, status: unsupported}
  - {id: dns.doh3.query.nodata, status: unsupported}
  - {id: dns.doh3.query.nxdomain, status: unsupported}
  - {id: dns.doh3.interoperability.query.a, status: unsupported}
  - {id: dns.doq.query.a, status: unsupported}
  - {id: dns.doq.interoperability.query.a, status: unsupported}
supportedTestSelectors: [{selectorType: test-id, expression: dns.dot.query.a}, {selectorType: test-id, expression: dns.dot.interoperability.query.a}, {selectorType: test-id, expression: dns.dot.resolver.interoperability.query.a}]
supportedScenarioSelectors: [{selectorType: scenario-id, expression: dns.dot.query.a}, {selectorType: scenario-id, expression: dns.dot.interoperability.query.a}, {selectorType: scenario-id, expression: dns.dot.resolver.interoperability.query.a}]
supportedProtocolFamilies: [dot]
supportedExecutionModes: [single-cell, process]
requiredTargetEndpointBindings: [{bindingId: primary, purpose: test-endpoint, endpointType: tls, protocols: [dot]}]
supportedArtifactTypes:
  - {type: validation.json, producedByStates: [stopped, failed]}
  - {type: result.json, producedByStates: [stopped, failed]}
  - {type: protocol-proof.json, producedByStates: [stopped, failed]}
  - {type: dns-wire-summary.json, producedByStates: [stopped, failed]}
  - {type: dns-resolver-summary.json, producedByStates: [stopped, failed]}
  - {type: upstream-proof.json, producedByStates: [stopped, failed]}
  - {type: tls-negotiation.json, producedByStates: [stopped, failed]}
  - {type: dns-dot-executor-result.json, producedByStates: [stopped, failed]}
  - {type: dns-load-summary.json, producedByStates: [stopped, failed]}
  - {type: load.stdout.log, producedByStates: [stopped, failed]}
  - {type: load.stderr.log, producedByStates: [stopped, failed]}
metricsAvailability: {available: true, availableKinds: [validation, queries-per-second, latency-percentiles, operation-outcomes, bytes, tls-negotiation]}
parser:
  id: protocol-lab-dns-dot-json
  type: protocol-lab-dns-dot-json
  preservesRawOutput: true
telemetry: {supported: true, supported_contract_versions: [telemetry-bundle-v1], supported_scopes: [test-executor, load-generator], supported_artifact_kinds: [summary, metrics, stdout, stderr, raw], export_modes: [inline, artifact-bundle], metadata: {optional: true, implementation_owned: true, description: The executor emits exact DNS wire and TLS negotiation proof; the runner owns stdout/stderr and saturation telemetry.}}
