schemaVersion: "1.0"
id: quic.transport.zero-rtt-rejected
title: QUIC Zero-RTT Rejected
name: QUIC Zero-RTT Rejected
family: quic.transport
description: Validate that replay-sensitive QUIC zero-RTT early data is rejected on a resumed session and the connection continues without early-data acceptance.
status: stable
kind: protocolValidation
layer: transport
protocol: quic
roles:
  - server
requires:
  capabilities:
    - quic.server
    - quic.resumption
    - quic.zero-rtt
  protocols:
    - quic
  roles:
    - server
trafficShape: bidirectional-stream
implementationRole: server
requiredCapabilities:
  - quicTransport
  - quicHandshake
  - quicResumption
  - quicZeroRtt
quicTransport:
  behavior: zero-rtt-rejected
  connectionCount: 1
  streamType: bidirectional
  streamCount: 1
  payloadSizeBytes: 1024
  payloadDirection: client-to-server
  openPattern: sequential
  expectedBytes: 0
validation:
  required: true
  checks:
    - zero-rtt-rejected
    - early-data-not-committed
    - resumed-session-completes
    - replay-sensitive-traffic-rejected
    - protocol:quic
  quic:
    handshakeMustComplete: true
    session:
      resumptionState: resumed
      earlyData: rejected
      replaySafety: replay-sensitive
benchmark:
  warmupSeconds: 5
  durationSeconds: 30
  repetitions: 1
  connections:
    - 1
  streamsPerConnection:
    - 1
benchmarkCompat:
  compatibleLoadShapes:
    - quic-zero-rtt
    - smoke
    - regression
    - comparison
  primaryMetrics:
    - handshakeDurationP50
    - handshakeDurationP95
    - connectionSuccessRate
    - errorRate
artifacts:
  required:
    - validation.json
    - result.json
  optional:
    - protocol-diagnostic-artifact
    - counters
networkProfile: reordering-1pct
requiredMetrics:
  - quic.handshake.durationMs
  - quic.connection.successCount
  - quic.stream.bytesSent
artifactRequirements:
  - load-generator.stdout.txt
  - load-generator.stderr.txt
  - protocol-diagnostic-artifact
tags:
  - phase6
  - quic
  - handshake
  - resumption
  - zero-rtt
  - replay-sensitive
