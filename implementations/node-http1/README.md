# Node.js node:http HTTP/1 Origin

Independent HTTP/1.1 origin using Node.js `node:http`. The package runs in the
official Node 24.4.1 Alpine OCI image pinned by digest.

```powershell
pwsh ./scripts/package/Build-NodeHttp1Package.ps1
```
